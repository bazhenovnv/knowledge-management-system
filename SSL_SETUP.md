# 🔒 Настройка HTTPS (SSL сертификат)

## Требование: у вас должен быть домен

Для настройки HTTPS нужен домен (например, `example.com`). Если домена нет, сначала:
1. Купите домен у любого регистратора
2. В настройках домена добавьте A-запись на IP: `89.169.47.23`

## Шаг 1: Настройка SSL на сервере

Подключитесь к серверу:

```bash
ssh root@89.169.47.23
```

Запустите установку SSL (замените `example.com` на ваш домен):

```bash
export DOMAIN=example.com
curl -fsSL https://raw.githubusercontent.com/YOUR_GITHUB_USERNAME/YOUR_REPO_NAME/main/scripts/setup-ssl.sh | bash
```

Скрипт автоматически:
- ✅ Получит бесплатный SSL сертификат от Let's Encrypt
- ✅ Настроит автоматическое перенаправление HTTP → HTTPS
- ✅ Настроит автообновление сертификата (каждые 60 дней)

## Шаг 2: Готово!

Теперь ваш сайт доступен по HTTPS:
- 🔒 **https://ВАШ_ДОМЕН**

HTTP запросы автоматически перенаправляются на HTTPS.

---

## Проверка статуса SSL

```bash
ssh root@89.169.47.23 "certbot certificates"
```

## Ручное обновление сертификата (опционально)

```bash
ssh root@89.169.47.23 "certbot renew"
```

Автообновление уже настроено, обычно это не требуется!
