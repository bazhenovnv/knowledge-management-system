# 🚀 Инструкция по развёртыванию

## Шаг 1: Создайте SSH ключ на сервере

Подключитесь к серверу:
```bash
ssh root@89.169.47.23
```

Создайте новый SSH ключ для GitHub Actions:
```bash
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_deploy
```

Нажмите **Enter** три раза (не указывайте пароль).

---

## Шаг 2: Скопируйте ключи

### Скопируйте приватный ключ (для GitHub секрета):
```bash
cat ~/.ssh/github_deploy
```

Выделите и скопируйте **весь текст** (начиная с `-----BEGIN OPENSSH PRIVATE KEY-----` до `-----END OPENSSH PRIVATE KEY-----`).

### Скопируйте публичный ключ:
```bash
cat ~/.ssh/github_deploy.pub
```

### Добавьте публичный ключ в authorized_keys:
```bash
cat ~/.ssh/github_deploy.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

---

## Шаг 3: Добавьте секреты в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**

### Добавьте 3 секрета:

#### 1. SSH_PRIVATE_KEY
- Name: `SSH_PRIVATE_KEY`
- Secret: **вставьте приватный ключ** из команды `cat ~/.ssh/github_deploy`
- Нажмите **Add secret**

#### 2. SSH_HOST
- Name: `SSH_HOST`
- Secret: `89.169.47.23`
- Нажмите **Add secret**

#### 3. SSH_USERNAME
- Name: `SSH_USERNAME`
- Secret: `root`
- Нажмите **Add secret**

---

## Шаг 4: Запустите деплой

Теперь сделайте любой коммит в main ветку:

```bash
git add .
git commit -m "setup deployment"
git push origin main
```

Или нажмите **Actions** → **Deploy** → **Run workflow** в GitHub.

---

## ✅ Готово!

Ваш сайт будет автоматически публиковаться по адресу:
- **http://89.169.47.23**

При каждом push в main:
- ✅ Автоматически соберётся проект
- ✅ Автоматически загрузится на сервер
- ✅ Сайт обновится

---

## 🔒 Настройка HTTPS (если есть домен)

Если у вас есть домен, смотрите инструкцию в файле `SSL_SETUP.md`.
