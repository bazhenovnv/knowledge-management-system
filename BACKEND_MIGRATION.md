# 🚀 Миграция Backend на свой хостинг

## 📌 Текущая ситуация

Твой проект использует:
- ✅ **База данных** - Timeweb Cloud PostgreSQL (твоя, без ограничений)
- ⚠️ **Backend функции** - poehali.dev Cloud Functions (есть лимиты, 402 ошибка)
- ✅ **Frontend** - редактируется в poehali.dev, публикуется там же

---

## 🎯 Цель миграции

Перенести backend на твой хостинг, чтобы:
1. Убрать лимиты на количество запросов к API
2. Получить полный контроль над backend
3. Продолжать редактировать код в poehali.dev

---

## 📦 Что создано

В папке `standalone-backend/` находится готовый Flask API сервер:

```
standalone-backend/
├── app.py                  # Главный файл сервера
├── requirements.txt        # Зависимости Python
├── README.md              # Полная документация
├── QUICKSTART.md          # Быстрый старт
├── Dockerfile             # Для Docker (опционально)
├── docker-compose.yml     # Для Docker Compose (опционально)
├── .env.example           # Пример конфигурации
└── .gitignore            # Исключения для Git
```

---

## ⚡ Быстрый старт

### 1. Скачай папку `standalone-backend`

Через GitHub:
- Подключи GitHub в poehali.dev (Скачать → Подключить GitHub)
- Клонируй репозиторий к себе
- Возьми папку `standalone-backend`

Или скачай код вручную:
- Скачай → Скачать код в poehali.dev
- Извлеки папку `standalone-backend`

### 2. Размести на хостинге

**Рекомендую Timeweb Cloud (проще всего):**

1. Создай приложение Python на [timeweb.cloud](https://timeweb.cloud)
2. Загрузи `app.py` и `requirements.txt`
3. Команда запуска: `gunicorn -w 4 -b 0.0.0.0:$PORT app:app`
4. Получи URL (например, `https://твой-api.twc1.net`)

**ВАЖНО:** Добавь IP твоего API сервера в белый список базы данных:
- Timeweb Cloud → База данных → Настройки → Разрешённые IP
- Добавь IP твоего API сервера

### 3. Подключи к проекту

В poehali.dev создай файл `.env` в корне:
```env
VITE_EXTERNAL_DB_URL=https://твой-api.twc1.net
```

Пересобери проект.

### 4. Проверь работу

```bash
# Проверка здоровья API
curl https://твой-api.twc1.net/health

# Проверка подключения к БД
curl -X POST https://твой-api.twc1.net \
  -H "Content-Type: application/json" \
  -d '{"action": "stats"}'
```

---

## 📖 Подробная документация

Смотри файлы:
- `standalone-backend/QUICKSTART.md` - быстрый старт
- `standalone-backend/README.md` - полная документация с примерами

---

## 🔒 Безопасность

### Обязательно сделай:

1. **Перенеси пароли в переменные окружения**
   - Создай `.env` файл на сервере
   - Не коммить `.env` в Git!

2. **Настрой HTTPS**
   - Используй Let's Encrypt или SSL от хостинга

3. **Ограничь CORS** (опционально)
   - В `app.py` укажи только свой домен

---

## ❓ FAQ

### Где взять URL API после размещения?
- Timeweb Cloud даст URL вида `https://твой-проект.twc1.net`
- VPS: твой домен + настроенный Nginx
- Docker: IP сервера + порт (например, `http://123.45.67.89:8000`)

### Нужно ли удалять папку backend/ из проекта?
Нет, оставь её. Она больше не используется, но не мешает.

### Как обновить код API?
1. Измени `standalone-backend/app.py` на своём хостинге
2. Перезапусти сервер
3. В проекте poehali.dev ничего менять не нужно

### Можно ли использовать другой язык вместо Python?
Да! Можешь переписать API на Node.js, PHP, Go - главное, чтобы совпадал формат запросов/ответов.

---

## 🎉 Результат

После миграции:
- ✅ Продолжаешь редактировать код в poehali.dev
- ✅ База данных - твоя (Timeweb Cloud)
- ✅ Backend API - твой (без лимитов)
- ✅ Frontend - публикуется на poehali.dev или твоём домене
- ✅ Никаких ошибок 402 Payment Required

---

## 📞 Поддержка

Если возникли вопросы:
1. Изучи `standalone-backend/README.md`
2. Проверь логи на хостинге
3. Тестируй через `curl` или Postman

Успешной миграции! 🚀
