# 🔧 Инструкции по исправлению билда

## Проблема
После билда все запросы идут на относительный путь `/api/external-db` вместо прямого URL Cloud Function, что вызывает ошибки 501 и 500.

## Корневая причина
Vite кэширует environment variables и старые fallback URLs в коде.

## ✅ Что было исправлено

### 1. **apiConfig.ts** - Исправлен fallback для LEGACY_DATABASE
```typescript
// БЫЛО (плохо):
LEGACY_DATABASE: import.meta.env.VITE_LEGACY_DB_URL || 'https://ab-education.ru/api/external-db'

// СТАЛО (хорошо):
LEGACY_DATABASE: import.meta.env.VITE_LEGACY_DB_URL || 'https://functions.poehali.dev/72034790-df65-4fb9-885e-c40a2ee29179'
```

### 2. **externalDbService.ts** - Упрощена логика определения URL
```typescript
// БЫЛО:
const configUrl = API_CONFIG.EXTERNAL_DB;
const EXTERNAL_DB_URL = configUrl.startsWith('http') 
  ? configUrl 
  : 'https://functions.poehali.dev/72034790-df65-4fb9-885e-c40a2ee29179';

// СТАЛО:
const EXTERNAL_DB_URL = API_CONFIG.EXTERNAL_DB;
```

### 3. **testsService.ts** - Упрощена логика определения URL
```typescript
// БЫЛО:
const configUrl = API_CONFIG.EXTERNAL_DB;
this.directApiUrl = configUrl.startsWith('http') 
  ? configUrl 
  : 'https://functions.poehali.dev/72034790-df65-4fb9-885e-c40a2ee29179';

// СТАЛО:
this.directApiUrl = API_CONFIG.EXTERNAL_DB;
```

### 4. **autoRefreshService.ts** - Уже исправлено ранее
Все запросы MAX() теперь включают `WHERE IS NOT NULL` для предотвращения ошибок с пустыми колонками.

### 5. **backend/external-db/index.py** - Уже исправлено ранее
- DELETE метод правильно парсит queryStringParameters
- Добавлена cascade deletion для тестов

### 6. **.env.production** - Корректные URLs
Все переменные окружения указывают на правильный Cloud Function URL.

### 7. **.env** - Создан для development режима
Гарантирует одинаковые URL в dev и production.

## 🚀 Как применить исправления

### Вариант 1: Автоматическая проверка перед билдом
```bash
node verify-env.js
```
Скрипт проверит правильность `.env.production` перед билдом.

### Вариант 2: Просто пересобрать проект
Все исправления уже в коде, достаточно просто сделать билд через UI poehali.dev.

## ✅ После билда ожидаем:

1. ✅ Все запросы идут на `https://functions.poehali.dev/72034790-df65-4fb9-885e-c40a2ee29179`
2. ✅ DELETE метод работает корректно (возвращает 200, а не 501)
3. ✅ AutoRefreshService не выдаёт ошибки `column "created_at" does not exist`
4. ✅ Удаление тестов работает с cascade deletion
5. ✅ Нет запросов на `/api/external-db` или `ab-education.ru/api/external-db`

## 🔍 Как проверить в консоли после билда:

1. Открыть DevTools (F12)
2. Вкладка Network
3. Все запросы к БД должны идти на:
   ```
   https://functions.poehali.dev/72034790-df65-4fb9-885e-c40a2ee29179
   ```
4. DELETE запросы должны возвращать 200 OK, а не 501

## 📋 Чеклист проверки:

- [ ] Запросы идут на `functions.poehali.dev`, а не на `/api/external-db`
- [ ] DELETE тестов возвращает 200 OK
- [ ] AutoRefreshService работает без ошибок
- [ ] В консоли нет ошибок 501 или 500
- [ ] Тесты удаляются корректно (с cascade deletion)

---

**Дата исправления:** 2025-12-11  
**Коммит с исправлениями:** Следующий билд после этого файла
