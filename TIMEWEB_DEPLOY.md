# 🚀 Инструкция по развертыванию на Timeweb Cloud

## Быстрый старт

### 1. Создание приложения

1. Зайди на [Timeweb Cloud](https://timeweb.cloud/my/apps)
2. Нажми **"Создать приложение"**
3. Выбери **"Из GitHub репозитория"**

### 2. Настройки Git

| Параметр | Значение |
|----------|----------|
| **Репозиторий** | `bazhenovnv/knowledge-management-system` |
| **Ветка** | `main` ⚠️ **НЕ выбирай конкретный коммит!** |

### 3. Настройки сборки

| Параметр | Значение |
|----------|----------|
| **Framework** | Docker |
| **Путь к Dockerfile** | _оставь пустым_ (файл в корне) |
| **Root directory** | _оставь пустым_ |
| **Port** | `8000` |

### 4. Конфигурация сервера

Минимальная конфигурация:
- **CPU**: 1x 3.0THz
- **RAM**: 2 ГБ
- **SSD**: 20 ГБ

### 5. Переменные окружения

**Ничего добавлять не нужно!** 

Все данные БД уже прописаны в коде (`app.py`):
```python
DB_CONFIG = {
    'host': 'c6b7ae5ab8e72b5408272e27.twc1.net',
    'port': '5432',
    'dbname': 'default_db',
    'user': 'gen_user',
    'password': 'TC>o0yl2J_PR(e'
}
```

### 6. Запуск

Нажми **"Создать приложение"** и дождись завершения сборки.

## Проверка работы

После успешного деплоя:

### Health Check
```bash
curl https://your-app-url.twc1.net/health
```

Ответ:
```json
{
  "status": "ok",
  "service": "knowledge-management-api"
}
```

### Тестовый запрос к БД
```bash
curl https://your-app-url.twc1.net/?action=stats
```

## Устранение проблем

### ❌ Ошибка: "failed to read dockerfile"

**Причина**: Timeweb использует старый коммит

**Решение**:
1. Убедись, что в настройках выбрана **ветка `main`**, а не коммит
2. Нажми "Пересобрать"

### ❌ Ошибка: "Database connection failed"

**Причина**: Проблемы с SSL сертификатом или доступом к БД

**Решение**:
1. Проверь, что БД на Timeweb запущена
2. Проверь настройки `DB_CONFIG` в `app.py`
3. Проверь логи приложения в панели Timeweb

### ❌ Port 8000 not responding

**Причина**: Приложение не запустилось

**Решение**:
1. Проверь логи в Timeweb панели
2. Убедись, что `gunicorn` установлен в `requirements.txt`
3. Проверь синтаксис в `app.py`

## Структура проекта

```
.
├── Dockerfile              # Docker конфигурация
├── app.py                  # Flask API сервер
├── requirements.txt        # Python зависимости
└── standalone-backend/     # Резервная копия файлов
    ├── app.py
    ├── requirements.txt
    └── Dockerfile
```

## API Endpoints

### GET /health
Проверка работоспособности сервиса

### GET|POST /?action=query
Выполнение произвольного SQL запроса
```json
{
  "action": "query",
  "query": "SELECT * FROM schema.table LIMIT 10"
}
```

### GET|POST /?action=list
Получение списка записей
```json
{
  "action": "list",
  "table": "employees",
  "schema": "t_p47619579_knowledge_management",
  "limit": 100,
  "offset": 0
}
```

### GET|POST /?action=stats
Статистика по таблицам
```json
{
  "action": "stats",
  "schema": "t_p47619579_knowledge_management"
}
```

### POST /?action=create
Создание новой записи
```json
{
  "action": "create",
  "table": "employees",
  "data": {
    "name": "Иван Иванов",
    "email": "ivan@example.com"
  }
}
```

### PUT /?action=update
Обновление записи
```json
{
  "action": "update",
  "table": "employees",
  "id": 1,
  "data": {
    "name": "Иван Петров"
  }
}
```

### DELETE /?action=delete
Удаление записи
```json
{
  "action": "delete",
  "table": "employees",
  "id": 1
}
```

## Полезные ссылки

- [Timeweb Cloud панель](https://timeweb.cloud/my/apps)
- [GitHub репозиторий](https://github.com/bazhenovnv/knowledge-management-system)
- [База данных Timeweb](https://timeweb.cloud/my/databases)

---

**Последнее обновление**: 26 ноября 2024  
**Коммит с рабочими файлами**: `ec44c54`
