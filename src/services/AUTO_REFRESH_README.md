# Система автоматического обновления данных

## Описание

Централизованная служба для автоматического обновления данных приложения. 
Проверяет изменения в базе данных каждые 30 секунд и уведомляет все подписанные компоненты только при наличии реальных изменений.

## Как это работает

1. **Служба autoRefreshService** каждые 30 секунд проверяет timestamp последних обновлений в таблицах БД
2. Если обнаружены изменения, служба уведомляет всех подписчиков
3. Каждый подписчик обновляет только свои данные

## Преимущества

- ✅ **Экономия ресурсов**: данные обновляются только при реальных изменениях
- ✅ **Централизованное управление**: одна служба для всех компонентов
- ✅ **Простая интеграция**: подписаться может любой компонент
- ✅ **Контроль**: можно включать/отключать автообновление

## Использование

### Подписка компонента на автообновление

```typescript
import { autoRefreshService } from '@/services/autoRefreshService';
import { useEffect } from 'react';

const MyComponent = () => {
  useEffect(() => {
    // Подписываемся на обновления
    autoRefreshService.subscribe('my-component-id', () => {
      console.log('Обновляем данные');
      loadMyData(); // Ваша функция загрузки данных
    });
    
    // Отписываемся при размонтировании
    return () => {
      autoRefreshService.unsubscribe('my-component-id');
    };
  }, []);
  
  // ... остальной код компонента
};
```

### Управление службой

```typescript
import { autoRefreshService } from '@/services/autoRefreshService';

// Запустить автообновление
autoRefreshService.start();

// Остановить автообновление
autoRefreshService.stop();

// Получить статус
const status = autoRefreshService.getStatus();
console.log(status);
// {
//   isRunning: true,
//   listenersCount: 3,
//   checkInterval: 30000,
//   lastKnownUpdates: { employees: '2024-01-01T...' }
// }
```

## Подключенные компоненты

На данный момент подписаны:

1. **DataContext** (`data-context`) - обновляет общую статистику приложения
2. **DatabaseTestManagement** (`test-management`) - обновляет список тестов
3. **useNotifications** (`notifications`) - обновляет уведомления пользователя

## Отслеживаемые таблицы

- `employees` - сотрудники
- `tests` - тесты
- `test_results` - результаты тестов
- `courses` - курсы
- `notifications` - уведомления
- `knowledge_materials` - учебные материалы

## Компонент индикатора

Компонент `AutoRefreshIndicator` показывает статус автообновления в админ-панели:
- Статус (включено/выключено)
- Время последнего обновления
- Количество подписчиков
- Список отслеживаемых таблиц
- Кнопка включения/отключения

## Настройка

Изменить интервал проверки можно в файле `autoRefreshService.ts`:

```typescript
private checkInterval = 30000; // миллисекунды (30 сек)
```

## Отладка

Все действия службы логируются в консоль:
- ✓ Изменений не обнаружено
- 🔄 Обнаружены изменения в таблицах: [...]
- Подписчик [id] зарегистрирован
- Подписчик [id] отписан
- Уведомляем [N] подписчиков об обновлении
