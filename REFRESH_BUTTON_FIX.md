# Исправление кнопки "Обновить данные"

## Проблема
Кнопка "Обновить" на главной панели администратора не обновляла все службы и данные полностью. Она только увеличивала счетчик `analyticsRefresh`, который триггерил обновление некоторых компонентов аналитики.

**Что НЕ обновлялось:**
- Статистика из DataContext (количество сотрудников, средний балл, тесты)
- Топ сотрудников (TopEmployees)
- Уведомления (notifications)
- Управление тестами (DatabaseTestManagement)
- Другие подписчики autoRefreshService

## Решение

### 1. Добавлен публичный метод в autoRefreshService
**Файл:** `src/services/autoRefreshService.ts`

Добавлен метод `triggerManualRefresh()` для ручного запуска обновления всех подписчиков:

```typescript
async triggerManualRefresh() {
  console.log('🔄 Запущено ручное обновление всех служб');
  await this.notifyListeners();
}
```

### 2. Обновлена кнопка в AdminDashboard
**Файл:** `src/components/dashboard/AdminDashboard.tsx`

Теперь кнопка выполняет три действия:

```typescript
onClick={async () => {
  setIsRefreshing(true);
  try {
    // 1. Обновляем DataContext (статистика, сотрудники, тесты)
    await refreshData();
    
    // 2. Триггерим счетчики аналитики (DbRequestCounter, FunctionCallCounter)
    setAnalyticsRefresh(prev => prev + 1);
    
    // 3. Уведомляем всех подписчиков autoRefreshService
    await autoRefreshService.triggerManualRefresh();
    
    toast.success('Все данные обновлены');
  } catch (error) {
    console.error('Error refreshing:', error);
    toast.error('Ошибка обновления данных');
  } finally {
    setTimeout(() => setIsRefreshing(false), 500);
  }
}}
```

## Что теперь обновляется

При нажатии кнопки "Обновить данные" обновляются:

### DataContext (refreshData)
- ✅ Количество сотрудников (активных, неактивных, всего)
- ✅ Количество тестов
- ✅ Результаты тестов
- ✅ Средний балл (рассчитывается из percentage)
- ✅ Активные курсы
- ✅ Новые регистрации

### Счетчики аналитики (analyticsRefresh)
- ✅ DbRequestCounter - запросы к базе данных
- ✅ FunctionCallCounter - вызовы функций
- ✅ TopFunctionsWidget - топ функций
- ✅ FunctionAnalytics - детальная аналитика функций

### Подписчики autoRefreshService (triggerManualRefresh)
- ✅ data-context - контекст данных
- ✅ test-management - управление тестами
- ✅ notifications - уведомления
- ✅ top-employees - топ сотрудников

## Результат

Теперь кнопка "Обновить данные" полностью обновляет все данные на панели администратора:
- Статистические карточки вверху
- Счетчики аналитики
- Топ сотрудников
- Уведомления
- Все остальные компоненты, подписанные на autoRefreshService

Время обновления: ~0.5-1 секунда  
Визуальный индикатор: иконка вращается во время обновления  
Уведомление: toast "Все данные обновлены" при успехе
