# 🚀 Как подключить свой Backend (вместо poehali.dev)

## ❗ Зачем это нужно?

Сейчас твой проект использует:
- ✅ **Твоя база данных** - Timeweb Cloud PostgreSQL
- ⚠️ **Backend poehali.dev** - есть лимиты (ошибка 402 Payment Required)

После подключения своего backend:
- ✅ **Твоя база данных** - Timeweb Cloud PostgreSQL
- ✅ **Твой backend** - на твоём хостинге, **без лимитов**

---

## 📦 Что уже готово?

В папке **`standalone-backend/`** находится полностью готовый Flask API сервер.

Тебе нужно только:
1. Разместить его на хостинге (10-15 минут)
2. Обновить одну строку в `.env`
3. Пересобрать проект

---

## ⚡ Пошаговая инструкция

### Шаг 1: Открой папку `standalone-backend`

Там 3 главных файла:
- **START_HERE.md** ← начни с этого
- **QUICKSTART.md** ← подробная инструкция
- **test_api.py** ← для тестирования API

### Шаг 2: Размести API на Timeweb Cloud

**Самый простой вариант (рекомендую):**

1. Зайди на [timeweb.cloud](https://timeweb.cloud)
2. Создай новое приложение → **Python**
3. Загрузи файлы из `standalone-backend/`:
   - `app.py`
   - `requirements.txt`
4. В настройках приложения укажи команду запуска:
   ```
   gunicorn -w 4 -b 0.0.0.0:$PORT app:app
   ```
5. **⚠️ ВАЖНО:** Добавь IP своего API сервера в белый список базы данных:
   - Timeweb Cloud → База данных → Настройки → Разрешённые IP
   - Добавь IP своего приложения (можно узнать в настройках приложения)
6. Получи URL (например, `https://твой-api.twc1.net`)

### Шаг 3: Протестируй API

Открой терминал и выполни:

```bash
# Установи зависимости (если нужно)
pip install requests

# Запусти тест
cd standalone-backend
python test_api.py https://твой-api.twc1.net
```

Если все 4 теста прошли ✅ - отлично, переходи дальше!

Если нет:
- Проверь логи приложения на Timeweb Cloud
- Убедись, что IP добавлен в белый список БД
- Перечитай `standalone-backend/QUICKSTART.md`

### Шаг 4: Подключи к проекту

**В poehali.dev:**

1. Создай файл **`.env`** в корне проекта (если ещё нет)
2. Добавь в него:
   ```env
   VITE_EXTERNAL_DB_URL=https://твой-api.twc1.net
   ```
3. Пересобери проект

**Готово!** 🎉

Твой сайт теперь использует твой собственный backend без лимитов.

---

## ✅ Проверка работы

1. Открой свой сайт в poehali.dev
2. Зайди в раздел "Сотрудники"
3. Проверь, что список загружается
4. Попробуй добавить/изменить/удалить сотрудника

Если всё работает - поздравляю! Миграция завершена. 🚀

---

## 📊 Что происходит под капотом?

```
[Браузер пользователя]
        ↓
[Frontend на poehali.dev]
        ↓
[Твой Flask API на Timeweb Cloud]  ← ТЫ КОНТРОЛИРУЕШЬ ЭТО
        ↓
[Твоя PostgreSQL база на Timeweb Cloud]  ← И ЭТО ТОЖЕ
```

Ты получаешь полный контроль над backend и базой данных!

---

## 🔒 Безопасность

### Обязательно сделай после размещения:

1. **Перенеси пароли БД в переменные окружения**
   - На Timeweb Cloud: Приложение → Настройки → Переменные окружения
   - Добавь: `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`
   - Обнови `app.py` чтобы использовать `os.environ.get()`

2. **Настрой HTTPS**
   - Timeweb Cloud автоматически выдаёт SSL сертификат
   - Убедись, что используешь `https://` URL

3. **Ограничь CORS** (опционально)
   - В `app.py` укажи только свой домен:
   ```python
   CORS(app, origins=['https://твой-сайт.com'])
   ```

---

## 💰 Стоимость

**Timeweb Cloud:**
- Приложение Python: от 200₽/месяц
- База данных PostgreSQL: от 200₽/месяц
- Итого: ~400₽/месяц за полный контроль

**Альтернативы:**
- VPS (DigitalOcean, Beget): от $5/месяц
- Docker на своём сервере: бесплатно (если есть сервер)

---

## ❓ FAQ

### Можно ли размещать не на Timeweb Cloud?
Да! Flask API работает на любом хостинге с Python 3.11+:
- VPS (Ubuntu, Debian)
- DigitalOcean
- AWS, Google Cloud
- Beget, Reg.ru
- Docker контейнер

### Нужно ли удалять папку `backend/` из проекта?
Нет, можешь оставить. Она больше не используется, но не мешает.

### Как обновить код API?
1. Измени `app.py` на хостинге
2. Перезапусти приложение
3. В проекте poehali.dev ничего менять не нужно

### Что делать, если хочу вернуться на poehali.dev backend?
Удали строку `VITE_EXTERNAL_DB_URL` из `.env` или закомментируй её.

### Можно ли написать backend на другом языке?
Да! Главное, чтобы API возвращало данные в том же формате. Смотри примеры в `standalone-backend/README.md`.

---

## 📞 Поддержка

Если что-то не работает:

1. **Изучи документацию:**
   - `standalone-backend/START_HERE.md`
   - `standalone-backend/QUICKSTART.md`
   - `standalone-backend/README.md`

2. **Проверь логи:**
   - Timeweb Cloud: Приложение → Логи
   - VPS: `sudo journalctl -u api -f`

3. **Протестируй API:**
   ```bash
   python standalone-backend/test_api.py https://твой-api.twc1.net
   ```

---

## 🎉 Готово!

Теперь у тебя:
- ✅ Полный контроль над backend и базой данных
- ✅ Никаких лимитов на запросы
- ✅ Продолжаешь редактировать код в poehali.dev
- ✅ Можешь масштабировать по своему усмотрению

**Поехали! 🚀**
