# ⚡ Быстрый старт

## 🎯 Шаг 1: Скачай файлы

Скачай папку `standalone-backend` из своего проекта на компьютер.

---

## 🎯 Шаг 2: Выбери способ размещения

### 🏠 Способ 1: Локальный тест (5 минут)

```bash
# Установи зависимости
pip install -r requirements.txt

# Запусти сервер
python app.py
```

Готово! API работает на `http://localhost:8000`

Проверь: открой в браузере `http://localhost:8000/health`

---

### ☁️ Способ 2: Timeweb Cloud (15 минут)

**Самый простой вариант для начала!**

1. Зайди на [timeweb.cloud](https://timeweb.cloud)
2. Создай новое приложение → Python
3. Загрузи файлы: `app.py`, `requirements.txt`
4. В настройках укажи команду запуска:
   ```
   gunicorn -w 4 -b 0.0.0.0:$PORT app:app
   ```
5. Получи URL (например, `https://твой-проект.twc1.net`)

**Готово!** У тебя есть URL API.

---

### 🐳 Способ 3: Docker (если умеешь)

```bash
# Собери образ
docker build -t my-api .

# Запусти контейнер
docker run -d -p 8000:8000 my-api
```

Или проще:
```bash
docker-compose up -d
```

---

## 🎯 Шаг 3: Подключи к проекту poehali.dev

После того как получил URL своего API (например, `https://твой-проект.twc1.net`):

### В poehali.dev:

Создай файл `.env` в корне проекта:
```env
VITE_EXTERNAL_DB_URL=https://твой-проект.twc1.net
```

Или обнови файл `src/config/apiConfig.ts`:
```typescript
export const API_CONFIG = {
  EXTERNAL_DB: 'https://твой-проект.twc1.net',
  // ... остальные настройки
}
```

**Пересобери проект** в poehali.dev.

---

## ✅ Проверка работы

1. **Проверь здоровье API:**
   ```bash
   curl https://твой-проект.twc1.net/health
   ```
   
   Должно вернуться:
   ```json
   {"status": "healthy", "database": "connected"}
   ```

2. **Проверь получение статистики:**
   ```bash
   curl -X POST https://твой-проект.twc1.net \
     -H "Content-Type: application/json" \
     -d '{"action": "stats", "schema": "t_p47619579_knowledge_management"}'
   ```

3. **Открой свой сайт** в poehali.dev - всё должно работать!

---

## 🎉 Готово!

Теперь:
- ✅ База данных - твоя (Timeweb Cloud)
- ✅ Backend - твой (на твоём хостинге)
- ✅ Никаких лимитов на запросы
- ✅ Ты продолжаешь редактировать код в poehali.dev

---

## 🆘 Проблемы?

### API не отвечает
- Проверь логи хостинга
- Проверь, что порт открыт
- Убедись, что gunicorn запущен

### Ошибка подключения к БД
- Проверь, что IP твоего сервера добавлен в белый список Timeweb Cloud БД
- Зайди в панель Timeweb Cloud → База данных → Настройки → Разрешённые IP
- Добавь IP своего API сервера (или `0.0.0.0/0` для разработки)

### CORS ошибка
- Проверь, что `flask-cors` установлен
- В `app.py` убедись, что `CORS(app)` присутствует

---

## 📞 Следующие шаги

1. **Настрой HTTPS** (если ещё не настроен)
2. **Перенеси пароли в .env** (для безопасности)
3. **Настрой мониторинг** (логи, алерты)
4. **Добавь кэширование** (Redis) для ускорения

Подробнее смотри в `README.md`
