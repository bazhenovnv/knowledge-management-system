# 🎨 Render.com - Простой деплой с бесплатным планом

## Почему Render?
- ✅ Полностью бесплатный план (без кредитной карты)
- ✅ Автоматический деплой из GitHub
- ✅ Поддержка Docker из коробки
- ✅ Бесплатный SSL сертификат
- ✅ Публичный URL: `your-app.onrender.com`

## Шаг 1: Регистрация

1. Перейди на https://render.com
2. Нажми **"Get Started"**
3. **"Sign up with GitHub"** → авторизуй Render

## Шаг 2: Подключение репозитория

1. В дашборде нажми **"New +"** → **"Web Service"**
2. Нажми **"Connect account"** (если первый раз)
3. Выбери репозиторий: **`bazhenovnv/knowledge-management-system`**
4. Нажми **"Connect"**

## Шаг 3: Настройка сервиса

Render автоматически найдёт Dockerfile. Заполни форму:

| Параметр | Значение |
|----------|----------|
| **Name** | `knowledge-management-api` (любое имя) |
| **Region** | Frankfurt (EU Central) - ближе к Timeweb БД |
| **Branch** | `main` |
| **Runtime** | Docker |
| **Instance Type** | **Free** (бесплатный) |

### ⚠️ Важные настройки:

**Dockerfile Path**: оставь пустым (автоопределение)

**Docker Command**: оставь пустым (используется CMD из Dockerfile)

## Шаг 4: Дополнительные настройки (опционально)

### Environment Variables (переменные окружения)

**Ничего добавлять не нужно** - все данные уже в коде!

Если захочешь вынести потом:
- Нажми **"Advanced"**
- Add Environment Variable:
  - `PORT=8000` (Render автоматически пробросит)

### Auto-Deploy

По умолчанию включён ✅ - каждый push в `main` = автодеплой

## Шаг 5: Деплой

1. Нажми **"Create Web Service"**
2. Render начнёт сборку (займёт 3-5 минут)
3. Смотри логи в реальном времени

## Шаг 6: Получение URL

После успешного деплоя:
- URL появится вверху: `https://your-app.onrender.com`
- Бесплатный SSL уже настроен ✅

## Проверка работы

### Health Check
```bash
curl https://your-app.onrender.com/health
```

Ответ:
```json
{
  "status": "ok",
  "service": "knowledge-management-api"
}
```

### Статистика БД
```bash
curl https://your-app.onrender.com/?action=stats
```

## ⚠️ Ограничения бесплатного плана

1. **Sleep режим**: После 15 минут бездействия засыпает
   - Первый запрос разбудит (займёт ~30 сек)
   - Для production лучше платный план ($7/мес)

2. **Ресурсы**:
   - 512 MB RAM
   - 0.1 CPU
   - Достаточно для разработки

3. **Месячный лимит**: 750 часов (достаточно для 1 приложения)

## Логи и мониторинг

### Просмотр логов:
1. Открой свой сервис в дашборде
2. Перейди в **"Logs"** (слева)
3. Логи обновляются в реальном времени

### Метрики:
1. Вкладка **"Metrics"**
2. График CPU, RAM, Response Time

## Автодеплой из GitHub

**Уже настроен!** При каждом push в `main`:
1. Render получает webhook
2. Автоматически собирает новый Docker образ
3. Деплоит без даунтайма (zero-downtime deploy)

### Отключить автодеплой:
Settings → Build & Deploy → Auto-Deploy: **No**

## Кастомный домен (опционально)

Бесплатно можешь привязать свой домен:

1. Settings → Custom Domain
2. Add Custom Domain: `api.yourdomain.com`
3. Добавь CNAME запись в DNS:
   ```
   CNAME api.yourdomain.com -> your-app.onrender.com
   ```
4. Render автоматически выпустит SSL сертификат

## Troubleshooting

### ❌ Build Failed

**Причина**: Ошибка в Dockerfile или зависимостях

**Решение**:
1. Проверь логи сборки
2. Убедись, что Dockerfile в корне
3. Проверь `requirements.txt`

### ❌ Service Unavailable

**Причина**: Приложение заснуло (free plan)

**Решение**:
1. Подожди 30 секунд (просыпается)
2. Или апгрейдь на Starter ($7/мес)

### ❌ Database Connection Failed

**Причина**: БД на Timeweb недоступна или SSL проблема

**Решение**:
1. Проверь, что БД запущена на Timeweb
2. Проверь настройки `DB_CONFIG` в `app.py`
3. Проверь логи: может SSL сертификат не загрузился

### ❌ Port Binding Error

**Причина**: Render ожидает переменную `PORT`

**Решение**: Добавь в `app.py` (в конце файла):
```python
if __name__ == '__main__':
    port = int(os.environ.get('PORT', 8000))
    app.run(host='0.0.0.0', port=port, debug=False)
```

## Мониторинг состояния

Render автоматически делает health checks:
- Каждые 30 секунд пингует `/` или `/health`
- Если не отвечает → перезапускает сервис

## Сравнение планов

| Параметр | Free | Starter ($7/мес) |
|----------|------|------------------|
| Sleep режим | Да | Нет |
| RAM | 512 MB | 512 MB |
| CPU | 0.1 | 0.5 |
| Часы | 750/мес | Безлимит |
| SSL | Да | Да |
| Custom Domain | Да | Да |

## Полезные команды

### Ручной редеплой (если нужно):
1. Dashboard → твой сервис
2. Manual Deploy → **Deploy latest commit**

### Откат к предыдущей версии:
1. Events (слева)
2. Найди нужный деплой
3. **Redeploy**

### Удаление сервиса:
Settings → Danger Zone → **Delete Web Service**

---

**Время настройки**: 3-5 минут  
**Сложность**: 1/10  
**Бесплатный план**: Да (без карты!)  
**Рекомендация**: ⭐⭐⭐⭐⭐

## Что выбрать: Railway или Render?

### Выбирай Railway если:
- Нужны логи в реальном времени
- Нужно больше ресурсов ($5 кредитов)
- Планируешь несколько сервисов

### Выбирай Render если:
- Нужен полностью бесплатный план
- Не критично засыпание через 15 мин
- Нужен простой интерфейс
